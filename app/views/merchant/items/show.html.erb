<%= link_to "#{@merchant.name}'s' Index Page", merchant_items_path(@merchant) %><br><br>
<center><h1><%= @item.name%></h1></center>
<p>Description: <%= @item.description%></p>
<p>Current Price: $<%= @item.unit_price%></p>

<%= link_to "Update #{@item.name}", edit_merchant_item_path(@merchant, @item) %><br><br>
